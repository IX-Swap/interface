version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install rimraf
        - yarn pre-install
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'