version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn add git+https://${CHARTING_LIBRARY_TOKEN}@github.com/InvestaX/charting_library.git#master
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
