import makeStyles from '@mui/styles/makeStyles'

export default makeStyles(theme => ({
  container: {
    marginLeft: theme.spacing(2.625),
    marginBottom: theme.spacing(1.125)
  },
  reasonDescription: {
    margin: theme.spacing(0, 0, 3, 4.25),
    maxWidth: 382
  }
}))
