import makeStyles from '@mui/styles/makeStyles'

export default makeStyles(theme => ({
  container: {
    color: 'inherit',
    fontSize: 'inherit',
    fontWeight: 'inherit',
    lineHeight: 'inherit'
  }
}))
