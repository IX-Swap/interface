import { Card } from '@material-ui/core'
import { styled } from '@material-ui/core/styles'

export const IssuanceLandingCard = styled(Card)(({ theme }) => ({
  backgroundColor: theme.palette.backgrounds.light,
  height: '100%'
}))
