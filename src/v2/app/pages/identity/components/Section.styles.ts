import { makeStyles } from '@material-ui/styles'

export default makeStyles(() => ({
  column: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center'
  },
  sectionHeader: {
    fontWeight: 'bold'
  }
}))
