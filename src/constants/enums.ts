export enum FeeAmount {
  LOW = 500,
  MEDIUM = 3000,
  HIGH = 10000,
}

export enum PAYOUT_STATUS {
  DRAFT = 'draft',
  ANNOUNCED = 'announced',
  SCHEDULED = 'scheduled',
  STARTED = 'started',
  ENDED = 'ended',
  DELAYED = 'delayed',
}